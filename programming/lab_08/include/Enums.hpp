#pragma once

enum colors {
    white,
    yellow,
    green,
    red,
    blue,
    orange,
    black,
};

enum el_type {
    _corner,
    _edge,
    _center,
};

enum orientation {
    straight,
    l_side,
    r_sight,
};

enum rotation {
    no_rotation,
    UD_rot,
    LR_rot,
};